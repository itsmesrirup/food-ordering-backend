package com.dass.foodordering.food_ordering_backend.model;

// By declaring it as 'public' in its own file, it becomes visible everywhere.
public enum PaymentModel {
    SUBSCRIPTION,
    COMMISSION
}